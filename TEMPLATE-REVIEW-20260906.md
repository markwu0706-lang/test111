# 指定秀米模板改版检查

范围：4 个合集、16 条产品详情，保留原 URL。首页、CRM、微信验证文件不变。

参考：用户指定 `r.xiumius.cn/board/v5/40fj7/667026829`，读取时内容为新疆线路合集；从该页进入 `a.xiumius.cn/board/v5/40fj7/715097800` 核对详情样式。公开原图、HTML、JSON与CSS存于工作区临时参考目录，不进入公开站点。

## 实施

- 独立页面只加载 `xiumi-template.css`，废止这些页面对旧叠加样式表的引用；旧文件保留作回滚。
- 手机满宽、电脑750px居中、单列海报。新疆合集用指定模板原封面；其他产品保持对应目的地照片，不能复制模板内别的线路。
- 去除自拟导读、英文栏目、官网头尾、章节导航、跨线路推荐及北京无原报价的占位块。
- 详情日序按参考90%缩放后的尺寸呈现；费用正文14px/1.8，独立行程与真实报价保留。
- 房间图片仍全宽原比例展示，不增加放大弹窗；大兴安岭无车辆图。
- 所有静态图片补真实width/height，北京动态图片也使用真实元数据。

## 检查结果

- 浏览器：20个URL × 320px / 390px / 1280px，共60组检查；无横向溢出、无全局头尾、所有主内容可见。实际纸页宽度分别320、390、750px。
- 浏览器：东北合集按Enter进入趣玩北极详情；北京非法参数进入明确错误状态，点击返回恢复北京合集。
- 浏览器：大兴安岭住宿5张图均contain、原始比例误差小于0.0002，完整显示。
- 静态独立审查：257个本地引用/片段有效；原图片包括房车全部保留；41天静态行程正文及北京37天行程未改。
- 报价：东北5张表的日期、成人、儿童、单房差、附加费用和脚注逐字不变。
- 96个独立图片文件经Sharp读取尺寸成功。全量本地HTTP资源探测受命令环境网络限制未完成；浏览器已加载的图片无失败，发布后另按公开文件校验。
- `node --check beijing-2026/route.js`、`node --check beijing-2026/route-data.js`、`git diff --check`通过。
- `designmd lint DESIGN.md`：0 errors；7项颜色未挂接机器可读组件的warning，本文只镜像CSS令牌而非代码生成源。
- 无原生alert/confirm/prompt、onclick或空href假操作。

## 边界

参考的标题字体、图片圆角和阴影已合成进栅格海报，无法从网页提取原字体文件；更换产品文字的页面是同类宋体及实测比例复刻，不宣称逐像素相同。只有新疆合集封面直接使用原图。房车原片不因模板样式重排而替换或假冒高清。

SH08和SH10原PDF仍缺失，不借用SH13行程补齐。Pexels可选署名不展示；需要保留的原摄影许可署名仍在相关图片区域。Lucide Pointer图标沿用已安装库的ISC许可路径。

发布包仅18个公开文件，修改前备份16个既有文件。新增CSS和新疆封面不覆盖其他公开资产；不含内部PDF、聊天记录或凭据。
